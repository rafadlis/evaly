{
  "name": "evaly",
  "private": true,
  "scripts": {
    "check-types": "tsc --noEmit",
    "shell:dev": "bunx sst shell --stage development",
    "shell:staging": "bunx sst shell --stage staging",
    "shell:production": "bunx sst shell --stage production",
    "db:studio": "bun --filter @evaly/backend db:studio",
    "dev": "bunx sst dev --stage=development --verbose",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "deploy:staging": "bunx sst deploy --stage staging --verbose",
    "deploy:production": "bunx sst deploy --stage production --verbose"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "workspaces": [
    "backend",
    "frontend"
  ],
  "dependencies": {
    "sst" :"3.9.26"
  }
}