{
  "name": "evaly",
  "private": true,
  "scripts": {
    "check-types": "tsc --noEmit",
    "db:generate": "bun --filter @evaly/backend db:generate",
    "db:migrate": "bun --filter @evaly/backend db:migrate",
    "db:studio": "bun --filter @evaly/backend db:studio",
    "dev": "bunx sst dev --stage=development --verbose",
    "format": "prettier --write \"**/*.{ts,tsx,md}\"",
    "deploy:staging": "bunx sst deploy --stage stagings --verbose",
    "deploy:production": "bunx sst deploy --stage production --verbose"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "workspaces": [
    "backend",
    "frontend"
  ],
  "dependencies": {
    "sst" :"3.9.26"
  }
}